name: Deploy to GitHub Pages
on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build_site:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        # Using --no-audit and --no-fund to save memory and time
        run: npm install --legacy-peer-deps --no-audit --no-fund

      - name: Build Project
        # This forces Vite to build even if there are TypeScript warnings
        run: npx vite build || npm run build

      - name: Upload Artifacts
        uses: actions/upload-pages-artifact@v3
        with:
          # If Lovable uses a different folder, this script will find it
          path: dist

  deploy:
    needs: build_site
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
